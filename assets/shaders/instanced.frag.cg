#version 450

layout(location = 0) out vec4 outColor;
layout(location = 1) out uint outMaterial;

layout(location = 0) flat in uint inInstance;

void main() {
	outMaterial = inInstance;
	outColor = vec4(inInstance * 0.2, 0.5, 0.0, 1.0);
}
